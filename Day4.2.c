#include<stdio.h>

void main()

 {  
      // is used for single line comment 
     // there will be no else without if 
    // there will be no statement between if and else body
 	int pin=1234,upin;
 	printf("enter pin");
 	scanf("%d",&upin);
 	if(pin==upin){
 		printf("Valid user");
	 }
	 else{
	 	printf("Invalid pin entered");
	 }
 	
 	
 }
